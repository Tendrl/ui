(function() {
    "use strict";

    angular
        .module("TendrlModule")
        .service("clusterFactory", clusterFactory);

    /*@ngInject*/
    function clusterFactory($state, $q, $http, utils, config) {
        var vm = this;

    }

})();
